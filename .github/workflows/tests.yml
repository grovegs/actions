name: 🧪 Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  dotnet_build_tests:
    uses: ./tests/dotnet-build-tests.yml

  godot_pack_tests:
    uses: ./tests/godot-pack-tests.yml

  nuget_pack_tests:
    uses: ./tests/nuget-pack-tests.yml

  setup_godot_tests:
    uses: ./tests/setup-godot-tests.yml
