name: 🧪 Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_dotnet_tests:
    uses: ./tests/build-dotnet-tests.yml

  pack_godot_tests:
    uses: ./tests/pack-godot-tests.yml

  pack_nuget_tests:
    uses: ./tests/pack-nuget-tests.yml

  setup_godot_tests:
    uses: ./tests/setup-godot-tests.yml
